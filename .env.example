# Optional — required only for Google Sheets read-only import.
# Do not commit real credentials. Leave empty for local CSV/roster development.
VITE_GOOGLE_OAUTH_CLIENT_ID=

# Optional hours-counter URL. Default is the Corresponding Cloudflare Worker.
# VITE_HOURS_COUNTER_URL=https://corresponding-hours.kylekimler.workers.dev
# VITE_HOURS_COUNTER_DISABLE=1
