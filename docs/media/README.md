# Demo recordings

Video files in this folder are **gitignored**. They are for local review and
for uploading to GitHub issue/PR attachments or release notes.

Incoming (from the founder’s desktop):

| File | Platform |
| --- | --- |
| `scholarone-demo.mov` | ScholarOne / Manuscript Central |
| `plos-one-demo.mov` | PLOS ONE / Editorial Manager |
| `medrxiv-demo.mov` | medRxiv / bioRxiv |

Copy them here as:

```text
docs/media/scholarone-demo.mov
docs/media/plos-one-demo.mov
docs/media/medrxiv-demo.mov
```

Do not commit the binaries. After they exist locally, upload each to GitHub
(comment or release) and paste the `user-attachments` URL into the README
“See it work” section.
